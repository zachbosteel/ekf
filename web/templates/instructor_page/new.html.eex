<h2>New instructor page</h2>

<%= render "form.html", changeset: @changeset,
                        action: instructor_page_path(@conn, :create) %>

<%= link "Back", to: instructor_page_path(@conn, :index) %>
